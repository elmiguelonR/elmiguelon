% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/article_translation.R
\name{search_and_translate_news}
\alias{search_and_translate_news}
\title{Search and Translate the summary of a single article.}
\usage{
search_and_translate_news(
  search_method = c("everything", "top-headlines"),
  keyword = NULL,
  from = NULL,
  to = NULL,
  sortBy = "relevancy",
  category = NULL
)
}
\arguments{
\item{search_method}{One of "everything" or "top-headlines" (default: "everything").}

\item{keyword}{Keyword to pass to \code{get_news_everything()} (required if \code{search_method = "everything"}).}

\item{from}{Start date (YYYY-MM-DD).}

\item{to}{End date (YYYY-MM-DD).}

\item{sortBy}{Sort order for \code{get_news_everything()} (e.g., "relevancy", "popularity").}

\item{category}{Category to pass to \code{get_top_headlines()} (e.g., "business", "technology").}
}
\value{
The translated text (invisible). Also prints the translated text to the console.
}
\description{
This function retrieves news articles either from "everything" or "top-headlines,"
displays them, prompts the user to choose one by index, and translates it via OpenAI.
}
\examples{
\dontrun{
# 1) Retrieve articles about "Apple" from March 1 to March 8, 2025 -> choose one -> translate to Korean:
search_and_translate_news(
  search_method = "everything",
  keyword = "Apple",
  from = "2025-03-01",
  to = "2025-03-08",
  sortBy = "relevancy"
)

# 2) Retrieve top US headlines (business category) -> choose one -> translate to English:
search_and_translate_news(
  search_method = "top-headlines",
  category = "business"
)
}
}
